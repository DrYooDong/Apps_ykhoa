{
  "name": "cliniportal",
  "version": "2.0.0",
  "description": "CliniPortal — Hệ sinh thái Web & App Y khoa Offline Đa nền tảng",
  "main": "desktop/main-electron.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-dist.js",
    "deploy": "npm run build && git subtree push --prefix dist origin gh-pages",
    "preview": "vite preview",
    "start": "electron desktop/main-electron.js",
    "desktop": "electron desktop/main-electron.js",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap add android",
    "cap:open:android": "npx cap open android"
  },
  "keywords": [
    "medical",
    "cliniportal",
    "calculators",
    "guidelines",
    "ebm",
    "offline-first",
    "electron",
    "capacitor",
    "pwa"
  ],
  "author": "CliniPortal Team",
  "license": "ISC",
  "devDependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "typescript": "^5.8.0",
    "vite": "^6.3.0"
  }
}
